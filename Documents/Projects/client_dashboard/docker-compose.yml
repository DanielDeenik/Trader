version: '3.8'\nservices:\n  client_dashboard:\n    build: .\n    ports:\n      - \"8050:8050\"\n    environment:\n      - OPENAI_API_KEY=\${OPENAI_API_KEY}\n      - AIRTABLE_API_KEY=\${AIRTABLE_API_KEY}\n            version: '3.8'
services:
  client_dashboard:
    build: .
    ports:
      - "8050:8050"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - AIRTABLE_API_KEY=${AIRTABLE_API_KEY}
