# Runtime dependencies for client_dashboard on Cloud Run.
#
# Pins are intentionally minimal: bump only where an upstream CVE
# forces the change or where the previous pin is too old to install
# cleanly on Python 3.11. Broader library upgrades (OpenAI SDK v1,
# pyairtable) are tracked as deferred follow-ups in the Cloud Run
# deploy spec — see docs/superpowers/specs/.

# --- Server -----------------------------------------------------------
gunicorn>=21.2.0

# --- Dash / Flask stack ----------------------------------------------
dash>=2.17.0,<3.0.0
dash-bootstrap-components>=1.5.0
plotly>=5.17.0
werkzeug>=3.0.0  # CVE-2023-25577 fix

# --- Legacy OpenAI client (<1.0 keeps ChatCompletion surface) --------
# Deferred: migrate callbacks to `openai>=1.0` + OpenAIClient.
openai>=0.28.1,<1.0.0

# --- Airtable ---------------------------------------------------------
# `airtable-python-wrapper` is archived upstream but still functional;
# migration to `pyairtable` is a deferred follow-up.
airtable-python-wrapper>=0.15.3

# --- Data / ML --------------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# --- Scraping / config ----------------------------------------------
beautifulsoup4>=4.12.0
requests>=2.31.0
pyyaml>=6.0  # still used by the committed config.yaml.example tooling
